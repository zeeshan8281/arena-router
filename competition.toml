# Single config source (spec §8). Grown as work packages need it — only [pi] so far
# (WP1 vendoring). Other tables (benchmark, models, smoke, full, budget, judge) land
# with the runner/pipeline that reads them.

[pi]
package = "@earendil-works/pi-coding-agent"   # provenance; installed from vendor/pi/pi.tgz only (D20)
version = "0.80.9"
sha256 = "93e35f204ebbdc0c55b699581c282eeb47d444827f7cbd5d2a8a86e92b75e180"  # of vendor/pi/pi.tgz
