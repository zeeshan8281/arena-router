# v2: the UI reads the static results/ JSON the pipeline commits (spec §7.3).
# Defaults to raw.githubusercontent of the v2 branch; override to point at a deploy
# that bundles results/ or a different branch/host.
VITE_RESULTS_BASE=https://raw.githubusercontent.com/zeeshan8281/arena-router/v2
