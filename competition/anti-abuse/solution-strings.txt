# Curated distinctive solution strings — the second half of the static tripwire
# (ci-anti-abuse §3a). One string per line; blank lines and #-comments ignored.
# The tripwire flags any submission diff whose ADDED lines contain one of these.
#
# What belongs here: strings that only someone who read a task's solution/ would
# know — specific flags, expected-output literals, hard-coded hashes/keys,
# distinctive constants from the reference solution. NOT generic tokens.
#
# Populate from the registry (maintainer task): for each of the 89 tasks in
# laude-institute/terminal-bench-2, pull solution/ + tests/ and pick the few
# longest/most-unique literals that would never appear in a general harness.
# Seeded empty on purpose — task-id matching (task-ids.txt) already covers lazy
# embedding; solution strings catch the obfuscated case and are added as we find
# real attempts. Keep entries specific enough to avoid false positives.
